#Two steps
#E
    #For all steps in kmeans cluster selection, use gaussian distribution
        #Calculate the mean=u and variance which is strd deviance squared 


#M


#Gauss Distribution
def gaussDist(x,mean,variance):
    import math
    pi=math.pi
   
    exponent=-(((x-mean)**2)/(2*variance))
    e=math.exp(exponent)
    divider=math.sqrt(2*math.pi*variance)
    
#Mixed Models
